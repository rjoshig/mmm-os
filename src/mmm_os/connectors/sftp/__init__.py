"""SFTP file source (Phase 9.3 — deferred; placeholder only).

Per-tenant directories, a file-naming contract, and optional PGP; reuses the
file-parsing machinery to land datasets like any other file source.
"""
